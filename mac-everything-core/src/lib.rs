pub mod indexer;

// FSEvents integration
pub mod fsevents;

// Search Query Parser
pub mod query_parser;

// Index persistence (binary snapshot)
pub mod persist;

// FFI boundary for Swift
pub mod ffi;
